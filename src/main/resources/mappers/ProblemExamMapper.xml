<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.chunfeng.dao.mapper.ProblemExamMapper">
    <!--字段映射-->
    <resultMap id="permissionRoleMap" type="com.chunfeng.dao.entity.PermissionRole">
        <result column="exam_id" property="permissionId"/>
        <result column="permission_id" property="permissionId"/>
    </resultMap>
    <!--条件查询关系-->
    <select id="selectAllProblemExam" resultType="com.chunfeng.dao.entity.ProblemExam"
            parameterType="com.chunfeng.dao.entity.ProblemExam">
        select id, exam_id, permission_id
        from problem_exam
        <where>
            <if test="id!='' and id!=null">
                id=#{id}
            </if>
            <if test="permissionId!='' and permissionId!=null">
                and permission_id=#{permissionId}
            </if>
            <if test="roleId!='' and roleId!=null">
                and role_id=#{roleId}
            </if>
        </where>
    </select>
    <!--根据关系ID查询关系信息-->
    <select id="selectAllProblemExamById" resultType="com.chunfeng.dao.entity.ProblemExam">
        select id, exam_id, permission_id
        from problem_exam
        <where>
            id in
            <foreach collection="ids" item="id" open="(" separator="," close=")">
                #{id}
            </foreach>
        </where>
        ;
    </select>
    <!--插入一条关系数据-->
    <insert id="insertProblemExam">
        insert into problem_exam (id, exam_id, permission_id, create_user, create_time)
        values (#{id}, #{examId}, #{permissionId}, #{createUser}, #{createTime});
    </insert>
    <!--根据关系ID修改一条关系数据-->
    <update id="updateProblemExamById">
        update problem_exam
        <set>
            <if test="examId!='' and examId!=null">
                exam_id=#{examId}
            </if>
            <if test="permissionId!='' and permissionId!=null">
                ,permission_id=#{permissionId}
            </if>
            <if test="updateUser!='' and updateUser!=null">
                ,update_user=#{updateUser}
            </if>
            <if test="updateTime!='' and updateTime!=null">
                ,update_time=#{updateTime}
            </if>
        </set>
        where id=#{id};
    </update>
    <!--根据关系ID批量解绑关系信息-->
    <delete id="deleteProblemExamById" parameterType="list">
        delete
        from problem_exam
        where
        id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>